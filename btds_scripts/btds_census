#!/bin/bash
ROOT="${BTDS_ROOT:-$(pwd)}"
OUT="$ROOT/vars_dynamic.toml"
echo ">> [L.VI] Conducting Sovereign Investigation..."
cat <<VAR_EOF > "$OUT"
sys_arch = "$(uname -m)"
sys_cores = $(nproc)
sys_home = "$HOME"
sys_root = "$ROOT"
VAR_EOF
echo ">> [OK] Dynamic variables written to $OUT"
